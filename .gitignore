.venv/
__pycache__/
*.py[cod]
.env

# Regenerable: `python data/build_corpus.py`. 45MB, no reason to version it.
data/corpus.jsonl
data/queries.jsonl

# Regenerable: `python index/build_index.py`
.qdrant/
.artifacts/
.cache/

.DS_Store
.pytest_cache/

# 141MB at 200k passages, over GitHub's 100MB limit and regenerable with
# `python data/build_corpus.py`. The image ships the prebuilt index, not the corpus.
data/corpus.deploy.jsonl
data/queries.deploy.jsonl
